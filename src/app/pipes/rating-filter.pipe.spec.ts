import { RatingFilterPipe } from './rating-filter.pipe';

describe('RatingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
